#include "ft_printf.h"
#include <stdio.h>
#include <stdlib.h>
int main(int argc, char **argv)
{

	unsigned int counter = 0;
	ft_printf("counter: %d\n", ft_printf("Hello, World!\n"));

	return (counter);
}